<?xml version="1.0" encoding="UTF-8"?>
<met:KNIMEMetaInfo xmlns:met="http://www.knime.org/2.9/metainfo">
  <met:element form="text" name="Author"/>
  <met:element form="date" name="Creation Date">26/6/2010</met:element>
  <met:element form="multiline" name="Comments">Shows usage of workflow variables.

Requirements: none

You need to enable the expert mode to use this feature 
(add "-Dknime.expert.mode=true" to the knime.ini file in the installation directory).

Workflow variables are defined using the context menu on the workflow project,
they can then be used in any node in the workflow.

Workflow variables are useful when used in conjunction with the 
KNIME reporting extension or the KNIME batch application as they can be
set using a simple command line switch (for the batch application) or a parameter
prompt during the report generation.</met:element>
</met:KNIMEMetaInfo>